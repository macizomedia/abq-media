/**
 * @module utils/checkpoint
 * Save/restore CLIContext at each state transition for resume support.
 * Populated in Phase 6.
 */

// TODO Phase 6: saveCheckpoint(), loadCheckpoint(), findLatestCheckpoint()
